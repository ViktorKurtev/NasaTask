@model IEnumerable<AsteroidViewModel>

<div style="padding-left: 30px; padding-right: 30px;">
    <div class="row">
        <div class="col-md-12 col-md-offset-1">
            <div class="panel panel-default panel-table">
                <div class="panel-heading">
                    <div class="row">
                        <div class="col col-xs-6">
                            <h3 class="panel-title">Asteroids</h3>
                        </div>
                    </div>
                </div>
                <div class="panel-body">
                    <table class="table table-striped table-bordered table-list">
                        <thead>
                            <tr style="font-size: 12px;">
                                <th>Id</th>
                                <th>Name</th>
                                <th>Nasa Jpl Url</th>
                                <th>Is Potentially Hazardous</th>
                                <th>Close Approach Count</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var asteroid in Model)
                            {
                                <tr style="font-size: 12px;">
                                    <td><a asp-controller="Home" asp-action="GetAsteroidDetails" asp-route-asteroidid="@asteroid.Id">@asteroid.Id</a></td>
                                    <td>@asteroid.Name</td>
                                    <td><a href="@asteroid.NasaJplUrl" target="_blank" rel="noopener noreferrer">@asteroid.NasaJplUrl</a></td>
                                    <td>@asteroid.IsPotentiallyHazardousAsteroid</td>
                                    <td>@asteroid.CloseApproachCount</td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
                @*<div class="panel-footer">
                        <div class="row">
                            <div class="col col-xs-4">
                                Page @Model.PageNumber of @Model.PageCount
                            </div>
                            <div class="col col-md-8">
                                <ul class="pagination hidden-xs pull-right">
                                    @for (int i = 0; i < Model.PageCount; i++)
                                    {
                                        var buttonClass = Model.PageNumber == i + 1 ? "active-page" : "";
                                        var linkClass = Model.PageNumber == i + 1 ? "active-link-class" : "inactive-link-class";
                                        var num = i + 1;

                                        <li class="@buttonClass @linkClass" style="display:inline-block;">
                                            <a class="user-page-link" asp-controller="Administrator" asp-action="ControlPanel" asp-route-page="@num">
                                                @num
                                            </a>
                                        </li>
                                    }
                                </ul>
                            </div>
                        </div>
                    </div>*@
            </div>

        </div>
    </div>
</div>